
  # JOFLOW - Community Aid Platform

  This is the JOFLOW application - an intelligent platform connecting Givers and Receivers in real-time.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  